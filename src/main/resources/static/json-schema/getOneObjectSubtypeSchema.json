{
  "$id": "http://localhost:8080/json-schema/getOneObjectSubtypeSchema.json",
  "base": "http://localhost:8080/api",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Object subtype schema",
  "description": "Object subtype schema corresponding to each object's DcType",
  "type": "object",
  "required": [
    "data"
  ],
  "properties": {
    "additionalProperties": false,
    "data": {
      "type": "object",
      "required": [
        "id",
        "type",
        "attributes"
      ],
      "properties": {
        "id": {
          "type": "string",
          "description": "Identifier of the object subtype as UUID"
        },
        "type": {
          "const": "object-subtype",
          "description": "The type of the object subtype data being returned."
        },
        "attributes": {
          "additionalProperties": true,
          "description": "Schema representing the object subtype associated with each object's DcType",
          "type": "object",
          "required": [
            "dcType",
            "acSubtype"
          ],
          "properties": {
            "dcType": {
              "enum": [
                "IMAGE",
                "MOVING_IMAGE",
                "SOUND",
                "TEXT"
              ],
              "description": "Type of object stored. Higher level than mediaType. https://tdwg.github.io/ac/termlist/#dc_type"
            },
            "acSubType": {
              "type": "string",
              "description": "A string describing the supporting specialized type of the resource's DcType. https://terms.tdwg.org/wiki/Audubon_Core_Term_List#ac:subtype"
            }            
          }
        }
      }
    }
  }
}